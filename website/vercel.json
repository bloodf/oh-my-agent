{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"installCommand": "bun --version && bun install --frozen-lockfile && bun install --cwd ../web --frozen-lockfile",
	"buildCommand": "bun run build"
}
